# src

Canonical source tree for active scripts.

## Layout

- `LimbExtender.lua` - primary module
- `UI_LimbExtender.lua` - UI bootstrap script
- `esp/` - ESP-related modules
- `manager/` - shared runtime manager
- `games/` - game-specific integration scripts

Legacy public paths are still available at repository root and top-level folders as compatibility loaders.
